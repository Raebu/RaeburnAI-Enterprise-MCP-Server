# Screenshots

This project currently has no graphical user interface. It is an MCP stdio server intended to be launched by an MCP-compatible host.

## Placeholder assets to add later

When a UI or dashboard is introduced, add screenshots for:

- Connector status view
- Approval queue
- Audit log explorer
- Tool allowlist/denylist settings
- Deployment health view

For now, the relevant demo artefacts are:

- `examples/mcp-client-config.json`
- `examples/demo-data.json`
- `docs/ARCHITECTURE.md`
